# Legacy UI (Archived)

The files in this folder are the pre-LibreChat stack:
- chat.html static client
- start-web-server.py proxy server
- START-HOME-LLM.ps1 launcher for Ollama + proxy + Automatic1111

They remain for reference only. The new supported stack lives at the repository root using Docker + LibreChat. No further changes are planned here.
